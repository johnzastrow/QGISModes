1. To achieve the new user mode feature (multiple config files) would it be best to create an all-new plugin rather than extending the current one?
2. How are plugins handled as they injection controls into the UI
3. Can the designer handle plugins is there a way to leave them installed and active, but hide their UI elements selectively?
4. There is a utility under the settings menu called Interface Customization. Can this utility or its output be used in any way here?
5. In the current plugin, some buttons remain grayed out. Why is that?

